import type { IPrizesWrapperProps } from '../../types';
import './PrizesWrapper.css';
declare const PrizesWrapper: ({ children, className, tagName: Tag, style, }: IPrizesWrapperProps) => JSX.Element;
export default PrizesWrapper;
